"""Signal handlers for the users app."""
from __future__ import annotations

# Reserved for future signals (e.g., audit logging on user creation).
# Imported by UsersConfig.ready() to ensure handlers are registered.
